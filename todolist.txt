Skidor API todo

Data & models
- Refactor models and schemas:
  - Add users
  - ??
- Refactor data seeding script (align with new models)

Features – API & services
- Order product service:
    - Add a user login (provider vs local dilemma)

- Order product service:
  - Model, schema, route, service
  - Auth/security (e.g. bcrypt where relevant)
  - Rate limiting and race conditions
  - Separate internal vs external endpoints??
- Auth: refactor with JWT

Other tasks
- Caching and throttling for LRU requests and spam-flags(Redis)
- Logging and metrics (Prometheus)
  - Internal endpoints for health/metrics
- Centralized error handling

Before deploying
- Make a dockerfile 

Priority order: 1 (models + schemas) → 2 (seeding) → 3 (order service + auth) →  4 (caching, logging, errors) → 5 (Dockerfile)